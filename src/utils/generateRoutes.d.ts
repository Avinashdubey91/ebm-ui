import type { SideNavigationMenuDTO } from "../types/menuTypes";
import type { RouteObject } from "react-router-dom";
export declare const generateRoutes: (menus: SideNavigationMenuDTO[]) => RouteObject[];
